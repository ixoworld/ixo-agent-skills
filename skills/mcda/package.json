{
  "name": "@qi-skills/mcda",
  "version": "1.1.0",
  "description": "Deterministic Multi-Criteria Decision Analysis Qi Skill for the Qi Flow Engine",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/src/handlers.js",
  "types": "dist/src/handlers.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/handlers.d.ts",
      "import": "./dist/src/handlers.js"
    }
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/tests/*.test.js\""
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/src/",
    "SKILL.md"
  ]
}
